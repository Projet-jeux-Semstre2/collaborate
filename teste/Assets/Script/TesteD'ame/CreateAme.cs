using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateAme : MonoBehaviour
{
    
   // si il y a une collision entre moi et un autre gameobjet
   // si ce gameobject a un tag "enemie"
   // si ce gameobject est en mode ''disséminé""
      //  alors on creer un gameobject "ame" au point de collision une seule fois
      // moi se retrouve en mode aggloméré au gameobject 
      // je n'ai plus acces au comportement du mode 'disséminé'
    
    // si ce gameobject est en mode ''aggloméré'' 
        // alors je prend le mode ""agglomére
        // je n'ai plus acces au comportement du mode 'disséminé'
        // je suis en mode 'aggloméré' a l'ame relié au gameobject de ma collision
        
}
